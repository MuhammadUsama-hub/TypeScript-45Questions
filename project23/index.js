"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// conditional cases
let car = {
    name: "Civic",
    color: "White",
    model: 2024,
    price: 5550000,
    capacity: "Six People",
};
// console.log(car)
console.log("our pridiction about car name : ", car.name === "Civic" ? true : false, `Actual Car name: ${car.name}`);
console.log("our pridiction about car Color : ", car.color === "White" ? true : false, `Actual Car Color: ${car.name}`);
console.log("our pridiction about car Model : ", car.model === 2024 ? true : false, `Actual Car Model: ${car.model}`);
console.log("our pridiction about car Price : ", car.price === 5550000 ? true : false, `Actual Car Price: ${car.price}`);
console.log("our pridiction about car Capacity : ", car.capacity === "Six People" ? true : false, `Actual Car name: ${car.capacity}`);
console.log("our pridiction about car name : ", car.name === "City" ? true : false, `Actual Car name: ${car.name}`);
console.log("our pridiction about car Color : ", car.color === "Black" ? true : false, `Actual Car Color: ${car.color}`);
console.log("our pridiction about car Model : ", car.model === 2004 ? true : false, `Actual Car model: ${car.model}`);
console.log("our pridiction about car Price : ", car.price === 450000 ? true : false, `Actual Car Price: ${car.price}`);
console.log("our pridiction about car Capacity : ", car.capacity === "Two People" ? true : false, `Actual Car Capacity: ${car.capacity}`);
